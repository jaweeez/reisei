# --- Secrets / local env (NEVER commit) ---
.env
.env.*.local
.env.local
.env.development.local
.env.production.local
# (.env.example IS committed — it's the placeholder template)

# --- Node / package managers ---
node_modules/
npm-debug.log*
yarn-error.log*
pnpm-debug.log*
.pnpm-store/

# --- Expo / React Native ---
.expo/
.expo-shared/
dist/
web-build/
*.orig.*
*.jks
*.p8
*.p12
*.key
*.mobileprovision
ios/
android/
# (ios/ and android/ are generated by `expo prebuild` — regenerate, don't commit)

# --- Next.js / Vercel (if a separate web/backend app is added) ---
.next/
.vercel/
out/

# --- Prisma (only if Prisma is used instead of raw pg) ---
# prisma keeps schema in-repo; ignore generated client if configured that way

# --- Build / cache / tooling ---
.turbo/
coverage/
*.tsbuildinfo
.eslintcache

# --- OS / editor cruft ---
.DS_Store
*.pem
Thumbs.db
.idea/
.vscode/*
!.vscode/extensions.json

# @generated expo-cli sync-2b81b286409207a5da26e14c78851eb30d8ccbdb
# The following patterns were generated by expo-cli

expo-env.d.ts
# @end expo-cli
.vercel
.env*
